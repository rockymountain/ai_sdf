---
id: DEV-013
kind: task
title: Restore provider neutrality in the measurement contract
status: implemented
version: 1
traceability_level: T2
owner: factory-maintainer
---

# Purpose

Restore provider neutrality in the M3 measurement capability by separating generic
measurement validation from concrete treatment selection.

# Upstream intent

- `PROB-002`
- `FR-006`
- `FR-007`
- `NFR-002`
- `CMP-003`
- `ADR-006`
- `ADR-008`

# T2 implementation boundary

DEV-013 is limited to:

- replacing hard-coded GPT-5.6 Sol / medium equality checks with explicit generic
  treatment structure and provider-neutral vocabulary validation;
- evaluating retained invocation treatment relative to the declaring window;
- retaining the existing M3 treatment as a current experiment choice;
- deterministic verification under `TEST-013`;
- traceability, current control-state, and factory-learning updates owned by
  `CTRL-CHANGE-008`.

No treatment key is synthesized. Requested reasoning effort may be explicit `null`
when unsupported or unrequested, consistent with FR-006 and ADR-006. Treatment drift
inside a declared window remains visible and makes exact token KPIs ineligible.

# Historical and current-state boundary

DEV-011 remains truthful historical delivery evidence for the then-locked M3
treatment. DEV-013 evolves the generic contract before the actual M3 measurement
window begins. It rewrites no historical report, telemetry, or operational evidence.

The M3 window remains not started. DEV-013 does not declare a task set, evidence
boundary, M3 baseline result, or M4 authority.

# Exclusions

DEV-013 does not include a provider adapter, `AIRuntimePort` redesign, runtime
capability framework, telemetry schema or operational database migration,
invocation/attempt/circuit/watchdog change, automatic routing, M4, Graphify, context
optimization, a live provider proof, or an M3 measurement-window declaration.

# T2 classification

`T2 CONFIRMED`: this corrects a material provider-neutral architecture and external
measurement-contract boundary. It requires the existing CMP-003 and ADR-006 design
plus focused ADR-008 and TEST-013 evidence; it is not a T1 treatment-value edit.
