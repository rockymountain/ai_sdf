---
id: DEV-015
kind: task
title: Make QG-003 and QG-005 policy authoritative
status: implemented
version: 4
traceability_level: T2
owner: factory-maintainer
---

# Purpose

Implement FR-009 in CMP-002 under accepted ADR-010, and verify the result with
TEST-015.

# Scope

- validate complete canonical traceability-level requirements and escalation
  settings, failing closed on any missing/malformed/unsupported field;
- bind QG-003 and QG-005 declarations to their canonical policy contracts via
  `knowledge/schemas/traceability-policy-gate.schema.json` and
  `knowledge/schemas/risk-attestation-gate.schema.json`;
- make `requires.risk_attestation` the sole authoritative applicability source,
  removing the prior hard-coded `if level in {"T1","T2"}` logic;
- govern attestation content and shape via QG-005 level contracts only;
- evaluate base and proposed governance independently for every PR transition
  (existing task, new task, and PR-relevant removed task) so proposed
  governance cannot self-waive applicable base controls;
- pin a migration-only bootstrap adapter to the exact accepted base
  `305d51446e3823f3b990668a2af120dc7b295d6e`;
- add focused deterministic regressions covering all of the above plus
  QG-001/QG-002/QG-004 and FR-003/ADR-003 provenance regression safety.

# Exclusions

DEV-015 does not automate human semantic classification, define QG-006 review
evidence, redesign QG-004, introduce a generic policy engine, or touch Increment C
(FR-010/ADR-011/DEV-016).

# T2 classification

`T2`: the change affects security, compliance, merge-blocking external contract,
reliability, and governance semantics.

# Lifecycle and M3 boundary

Implemented under Owner Gate B-A implementation authority
(`OWNER-ACCEPT-CTRL-CHANGE-015-INCREMENT-B-2026-09-26`), bound to the consolidated
design package `docs/phase1/phase1-consolidated-B0-package.md` and repository base
`305d51446e3823f3b990668a2af120dc7b295d6e`, through two independent-audit-driven
correction rounds:

At `9b1767f482067c9484eb7eb1ad6e4e60e5fb3492` this task's own canonical status
was `active` and TEST-015 was already (prematurely) `verified`. An independent
Codex audit found two separate issues there: the accompanying handoff claimed
DEV-015 was already `implemented`, which the committed canonical status did not
support; and TEST-015's `verified` status was not yet supported by the required
real-path B0 verification coverage. A first correction
(`48a094c205e199e1441a4d8a7ff7e99d81052b10`) added the missing T0-applicability
and independent base/proposed-evidence-contract regressions through the real
`validate_change_evidence` path (both passed against the existing enforcement
code unchanged) and only then set this task's status to `implemented`.

A second independent Codex audit of `48a094c…` found the pinned-bootstrap
legacy-gate precondition (B0 section 12.2) under-tested — missing duplicate,
wrong-type, and non-mapping negative cases — and, once those cases were added
test-first, exposed a real enforcement defect: the legacy-gate match used plain
dict equality, so a wrong-typed `deterministic: 1` or `blocks_merge: 1` (Python:
`1 == True`) incorrectly passed the exact-boolean precondition. This second
correction added a strict `_matches_legacy_gate()` helper (exact key set, and
`type(...) is bool` for the two boolean fields) in
`tools/traceability/validate.py`, replacing the plain `==`/`!=` legacy-shape
comparisons, and extended the regression suite accordingly.

That authority remains implementation-only: it does not close Increment B,
declare an M3 fixed set or measurement window, or authorize M4.
CTRL-CHANGE-009 remains retained/deferred historical design.
