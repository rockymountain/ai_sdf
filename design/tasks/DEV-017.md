---
id: DEV-017
kind: task
title: Implement controlled dual-runtime execution and purpose-scoped measurement
status: implemented
version: 1
traceability_level: T2
owner: factory-maintainer
---

# Purpose

Add a second controlled runtime adapter for Claude execution and evolve M3 treatment
and accounting so a fixed DEV may use Claude for implementation and Codex for
read-only audit without losing unified attribution.

The source base is the local CTRL-CHANGE-009 freeze commit:

```text
267bf5437f44d0337ed5d8a303a175201397a7e1
```

# Upstream intent

- `PROB-002`
- `FR-006`
- `FR-007`
- `FR-011`
- `NFR-002`
- `CMP-003`
- `CMP-004`
- `ADR-006`
- `ADR-007`
- `ADR-008`
- `ADR-012`

# T2 implementation boundary

DEV-017 is limited to:

- one `ClaudeRuntimeAdapter` conforming to the unchanged `AIRuntimePort` through the
  installed Claude Code native CLI's machine-readable noninteractive surface;
- explicit trusted-operator `codex|claude` selection without automatic routing;
- purpose-governed mutation/read-only capability enforcement;
- explicit purpose-scoped `MeasurementWindow` treatment;
- window-relative adapter/profile validation and undeclared-purpose fail-closed
  behavior;
- unified DEV usage plus purpose and runtime breakdowns;
- deterministic fake-process and temporary-database verification under `TEST-017`.

Provider-specific CLI/process/event semantics stay inside the Claude adapter.
Provider-native identifiers are never invented. Exact usage requires complete,
authoritative machine-readable provider components from which canonical input,
output, and total usage can be normalized. Missing usage remains unknown.

# Exclusions

DEV-017 did not itself authorize a live Claude or Codex invocation, operational
database mutation, DEV outcome finalization, the M3 measurement window, a fixed M3
task set, M4, automatic routing, a role registry, a general multi-agent coordinator,
a plugin framework, or a concrete Claude model as factory policy. The separately
authorized PRE-WINDOW adoption proof and this Project Owner closure supplied the
additional authority used to prove and accept the delivered capability.

# Closure evidence

- Deterministic `TEST-017` passed.
- Claude Code controlled-runtime adoption was proven PRE-WINDOW by invocations
  `17e23de7-3729-432e-9a42-877ae22a13b4` and
  `ad3fb002-e14f-4d35-a1e1-ae1983b5c12a`.
- Claude implementation capability was proven through governed attempt 1 and the
  independently verified canary checkpoint with SHA-256
  `f001555e4f04c7e3f1a1b37b0c12fb21a83e5e8d71122e852bf955cf723e0a25`.
- Codex read-only audit invocation `b88b6628-a20b-4792-863e-8c2d6bf7f5f0`
  found no `AIRuntimePort` redesign, automatic routing, or material blocker.
- Exact Claude canonical token evidence was proven prospectively after provider-
  neutral usage normalization. Original Stage A invocation
  `ed6f070a-b8df-414c-a85d-07fb3394c137` remains immutable with unknown usage.
- All live proof evidence is PRE-WINDOW and does not count as M3 baseline evidence.

# Current state

DEV-017 is implemented. The M3 measurement window remains not started, its fixed set
remains undeclared, the cost baseline remains not proven, and exact token KPIs remain
not decision-eligible.
