---
id: DEV-014
kind: task
title: Bind QG-001 and QG-002 declarations to existing enforcement
status: implemented
version: 1
traceability_level: T2
owner: factory-maintainer
---

# Purpose

Implement FR-008 in CMP-002 under the accepted ADR-009 and verify the result with
TEST-014.

# Implemented scope

- validate the QG-001 and QG-002 declarations via `load_structural_gates`;
- bind each declaration to its existing schema (`scan_artifacts`) or reference
  (`validate_traceability`) enforcement path;
- fail closed for required missing, malformed, disabled, duplicated, or unsupported
  declarations, at both the workspace/proposed state (`validate_traceability`) and
  the base state (`validate_change_evidence`), mirroring the ADR-004 base/proposed
  self-protection pattern;
- focused deterministic regressions in `test_quality_gate.py`.

# Exclusions

DEV-014 does not create schema or reference semantics, duplicate either validator,
introduce a generic policy engine, change QG-003/QG-005/QG-006, or remove the QG-004
compatibility adapter.

# T2 classification

`T2 CONFIRMED`: this change affects canonical governance authority, the merge-
blocking external contract, reliability, and compliance semantics. It touches
`design/decisions/**` (`t2_path_triggers`) and does not create schema/reference
semantics, duplicate either validator, or introduce a generic policy engine.

# Current state

DEV-014 is implemented and deterministically verified under Owner reference
`OWNER-ACCEPT-CTRL-CHANGE-013-INCREMENT-A-2026-09-25`; it awaits independent Codex
audit and Project Owner closure decision for CTRL-CHANGE-013.

# Lifecycle and M3 boundary

DEV-014 is PRE-WINDOW governance-stabilization work. It is not part of the M3
baseline cohort, does not declare an M3 fixed set or EvidenceBoundary, and does not
start the M3 measurement window. This activation does not accept or activate
DEV-015 or DEV-016, and does not resolve any ADR-011 evidence-source decision.
