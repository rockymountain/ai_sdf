---
id: DEV-007
kind: task
title: Implement the controlled AI runtime telemetry boundary
status: implemented
version: 1
traceability_level: T2
owner: factory-maintainer
---

# Scope

Implement the first Phase-1.0 batch of CMP-003 under ADR-006: the
`ControlledAIInvocation` gateway, provider-neutral `AIRuntimePort`, bootstrap
`CodexRuntimeAdapter`, exact-or-explicitly-unknown telemetry, local SQLite
operational evidence, deterministic export, canonical finite-watchdog policy/read,
and non-mutating capability enforcement for invocations without implementation
attempt identity.

The one authorized live proof is an `acceptance_validation` invocation for DEV-007
under a read-only capability profile. DEV-007 does not implement attempt reservation,
attempt consumption, retry, circuit state, successor-scope mechanics, or DEV-008.

# Authorized policy

The human-approved canonical value is
`autonomous_execution.max_invocation_seconds: 600`. Runtime code reads this value
from `constitution/policies.yaml` and has no numeric fallback.

# Completion rule

Implementation is complete only when TEST-007 records passing deterministic gates,
one successful tool-bearing live invocation, exact current-turn usage with cumulative
thread reconciliation evidence, and unchanged pre/post governed workspace state.
